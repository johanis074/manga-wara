{% extends 'base.html.twig' %}

{% block title %}Bons Plans{% endblock %}
{% block body %}
	<main class="bg">
        <section>
				<h2>Bons Plans</h2>
		
			{# Liste des livres filtrés #}
			<ul class="cards-container">
				{% for product in pagination %}
					<li class="card">
                        {% set routeName = product.type ~ 's_show' %}
                        <a href="{{ path(routeName, {'id': product.id}) }}">

							<h3>{{ product.name }}</h3>
							<img src="{{ asset('uploads/pictureProduct/' ~ product.picture) }}" class="card-img-top" alt="{{ product.name }}">
							<p>
								Prix:
								{{ product.price }}
								€
							</p>
						</a>
						<button type="button" class="add-to-cart" data-id="{{ product.id }}" data-type="{{ product.type }}">
							➕ Ajouter au panier
						</button>
					</li>
				{% else %}
					<p>Aucun livre trouvé pour ces filtres.</p>
				{% endfor %}
			</ul>


			{# Pagination #}
			<div class="pagination">
				{{ knp_pagination_render(pagination) }}
			</div>
		</main>
    {% block javascript %}
        {{ parent() }}
    {% endblock %}
	{% endblock %}

